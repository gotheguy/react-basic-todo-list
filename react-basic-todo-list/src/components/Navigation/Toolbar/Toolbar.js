import React from 'react';

import './Toolbar.css';

const toolbar = (props) => (
    <header className="Toolbar">
        <div className="AppName">
            <h3>TodoList</h3>
        </div>
    </header>
);

export default toolbar;